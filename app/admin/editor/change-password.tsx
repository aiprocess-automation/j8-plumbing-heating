'use client'

/**
 * V1: password rotation deferred. Buyers use the email-delivered
 * password until handoff. The component is kept as a typed export so
 * any imports compile, but it renders nothing.
 */
export function ChangePassword() {
  return null
}
